# ForgeGradle recommends a roomy heap and no daemon reuse across FG projects.
org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false

# Mod coordinates (kept in sync with mods.toml).
mod_id=originspellschools
mod_version=1.0.0
